pages:
  stage: deploy
  script:
    - echo "Publishing Pages..."
  artifacts:
    paths:
      - public
  only:
    - main
pages:
  stage: deploy
  script:
    - echo "Publishing static site to GitLab Pages"
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH'
